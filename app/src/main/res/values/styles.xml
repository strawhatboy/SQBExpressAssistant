<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="FullscreenTheme" parent="android:Theme.NoTitleBar">
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@null</item>
        <item name="metaButtonBarStyle">@style/ButtonBar</item>
        <item name="metaButtonBarButtonStyle">@style/ButtonBarButton</item>
    </style>

    <!-- Backward-compatible version of ?android:attr/buttonBarStyle -->
    <style name="ButtonBar">
        <item name="android:paddingLeft">2dp</item>
        <item name="android:paddingTop">5dp</item>
        <item name="android:paddingRight">2dp</item>
        <item name="android:paddingBottom">0dp</item>
        <item name="android:background">@android:drawable/bottom_bar</item>
    </style>

    <!-- Backward-compatible version of ?android:attr/buttonBarButtonStyle -->
    <style name="ButtonBarButton" />

    <style name="edittext_usr_style">
        <item name="android:background">@drawable/edittext_selector</item>
        <item name="android:drawableLeft">@drawable/edittext_usr_selector</item>
        <item name="android:padding">12dp</item>
        <item name="android:drawablePadding">8dp</item>
        <item name="android:textColorHint">@color/font_grey</item>
        <item name="android:textColor">@color/font_dark</item>
    </style>
    <style name="edittext_pwd_style">
        <item name="android:background">@drawable/edittext_selector</item>
        <item name="android:drawableLeft">@drawable/edittext_pwd_selector</item>
        <item name="android:padding">12dp</item>
        <item name="android:drawablePadding">8dp</item>
        <item name="android:textColorHint">@color/font_grey</item>
        <item name="android:textColor">@color/font_dark</item>
    </style>

    <style name="checkbox_white_style">
        <item name="android:button">@drawable/checkbox_white_selector</item>
        <item name="android:padding">8dp</item>
    </style>
</resources>
